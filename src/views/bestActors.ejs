<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mejores actores</title>
    <link rel='stylesheet' href='/css/style.css' />
</head>
<body>
    <figure>
        <a href="/"> <img src="/img/Argenflix.jpeg" alt="Logo Argenflix"></a>
    </figure>
    <h1>Actores mas galardonados</h1>
    <ul>
        <% for( let i = 0; i < actors.length; i++ ) { %>
        <li>
            <a href="/actors/detail/<%=actors[i].id%>"><%= actors[i].first_name %> <%= actors[i].last_name %></a> <br>
            <%= 'RATING: ' + actors[i].rating %> 
        </li>
        <% } %>
        
    </ul>
</body>
</html>