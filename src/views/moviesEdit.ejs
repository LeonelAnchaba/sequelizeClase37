<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title class="titulo">Editar Película</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <figure>
        <a href="/"> <img src="/img/Argenflix.png" alt="Logo Argenflix"></a>
      </figure>
    <h1 class="titulo">Película a Editar :  <%= Movie.title %></h1>
    <section class="formulario">
        <form action="/movies/update/<%= Movie.id %>?_method=PUT" method="POST">
            <label for="title">Título : </label>
            <p><input type="text" name="title" id="title" value="<%= Movie.title %>" required></p>
            <p style="color:red"><%= locals.errores && errores.title ? errores.title.msg : null %> </p>

            <label for="rating">Rating :</label>
            <p><input type="number" name="rating" id="rating" value="<%= Movie.rating %>" required></p>
            <p style="color:red"><%= locals.errores && errores.rating ? errores.rating.msg : null %> </p>

            <label for="awards">Premios :</label>
            <p><input type="number" name="awards" id="awards" value="<%= Movie.awards %>" required></p>
            <p style="color:red"><%= locals.errores && errores.awards ? errores.awards.msg : null %> </p>

            <label for="release_date">Fecha de estreno :</label>
            <p><input type="date" name="release_date" id="release_date" value="<%= Movie.release_date %>" required></p>
            <p style="color:red"><%= locals.errores && errores.release_date ? errores.release_date.msg : null %> </p>

            <label for="length">Duración :</label>
            <p><input type="number" name="length" id="" value="<%= Movie.length %>" required></p>
            <p style="color:red"><%= locals.errores && errores.length ? errores.length.msg : null %> </p>
            <br>
            <label for="genreID">Género : </label>
            <p>
                <select name="genre_id" id="" required>
                    <% for (let i = 0 ; i < genres.length; i++) { %>
                        <% if (genres[i].id == Movie.genre_id) { %>
                            <option value="<%= genres[i].id %>" selected>
                                <%= genres[i].name %>
                            </option>
                        <% } else { %>
                            <option value="<%= genres[i].id %>">
                                <%= genres[i].name %>
                            </option>
                        <% } %>
                    <% } %>
                    <!--  genres.forEach(genre => { %>
                    <option value=" genre.id %>"  if (Movie.genre_id == genre.id) { %> 'selected' %>< }; %>>< genre.name %></option>
                    < }); %> -->
                </select>
            </p>
            <br>
            <p> <button class="botonAgregar" type="submit">Actualizar</button>
        
            <a  class="botonVolver" href="/movies">Listado de Películas</a></p>
        </form>
    </section>
</body>

</html>