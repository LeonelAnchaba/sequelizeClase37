<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= actor.first_name %> <%= actor.last_name %> </title>
    <link rel='stylesheet' href='/css/style.css' />

</head>
<body>
    <figure>
        <a href="/"> <img src="/img/Argenflix.png" alt="Logo Argenflix"></a>
    </figure>    
    <h1><%= actor.name %> </h1>
    <br>
    <p>NOMBRE: <a><%= actor.first_name %></a> </p>
    <p>APELLIDO: <a><%= actor.last_name %></a> </p>
    <p>RATING: <a><%= actor.rating %></a> </p><br>

    <p>ACTUÓ EN:</p>
        <ul>
            <% for (let i = 0; i < actor.movies.length; i++) { %>
                <li>
                    <a href="/movies/detail/<%=actor.movies[i].id%>"><%= actor.movies[i].title %></a>
                </li>
            <% } %>
        </ul><br>

        <p>PELÍCULA FAVORITA: <a><%= actor.movie ? actor.movie.title : "No especificado" %></a></p><br><br>

    <a class="botonVolver" href="/actors">Volver a la lista</a>
    <a class="botonModificar" href="/actors/edit/<%= actor.id %>">Editar</a>
    <a class="botonBorrar" href="/actors/delete/<%= actor.id %>">Borrar</a>

</body>
</html>