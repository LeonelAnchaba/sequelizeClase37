<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.title %> </title>
    <link rel='stylesheet' href='/css/style.css' />

</head>
<body>
    <figure>
        <a href="/"> <img src="/img/Argenflix.jpeg" alt="Logo Argenflix"></a>
    </figure>
    <h1><%= movie.title %> </h1>    
    <br>
    <p>RATING: <%= movie.rating %> </p>
    <p>PREMIOS: <%= movie.awards %> </p>
    <p>DURACIÓN: <%= movie.length + ' min'%> </p>
    <p>ESTRENADA EL: <%= movie.release_date %> </p>
    <p>GÉNERO: <%= movie.genre ? movie.genre.name : "No especificado" %> </p><br>

        ACTORES:
        <ul>
            <% for (let i = 0; i < movie.actors.length; i++) { %>
                <li class="actores">
                    <%= movie.actors[i].first_name %> <%= movie.actors[i].last_name %>
                </li>
            <% } %>
        </ul>
  


    <a class="botonVolver" href="/movies">Volver a la lista</a>
    <a class="botonModificar" href="/movies/edit/<%= movie.id %>">Editar</a>
    <a class="botonBorrar" href="/movies/delete/<%= movie.id %>">Borrar</a>

</body>
</html>