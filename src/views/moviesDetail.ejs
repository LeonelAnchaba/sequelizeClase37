<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.title %> </title>
    <link rel='stylesheet' href='/css/style.css' />

</head>
<body>
    <figure>
        <a href="/"> <img src="/img/Argenflix.png" alt="Logo Argenflix"></a>
    </figure>
    <h1><%= movie.title %> </h1>    
    <br>
    <p>RATING: <a><%= movie.rating %></a> </p>
    <p>PREMIOS: <a><%= movie.awards %></a> </p>
    <p>DURACIÓN: <a><%= movie.length + ' min'%></a> </p>
    <p>ESTRENADA EL: <a><%= movie.release_date %></a> </p>
    <p>GÉNERO: <a><%= movie.genre ? movie.genre.name : "No especificado" %></a> </p><br>

    <p>ELENCO: </p>
        <ul>
            <% for (let i = 0; i < movie.actors.length; i++) { %>
                <li class="actores">
                    <a href="/actors/detail/<%=movie.actors[i].id%>"><%= movie.actors[i].first_name %> <%= movie.actors[i].last_name %>
                </li>
            <% } %>
        </ul><br><br>
  


    <a class="botonVolver" href="/movies">Volver a la lista</a>
    <a class="botonModificar" href="/movies/edit/<%= movie.id %>">Editar</a>
    <a class="botonBorrar" href="/movies/delete/<%= movie.id %>">Borrar</a>

</body>
</html>