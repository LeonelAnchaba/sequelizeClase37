<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Agregá un actor</title>
    <link rel='stylesheet' href='/css/style.css' />
</head>

<body>
    <figure>
        <a href="/"> <img src="/img/Argenflix.png" alt="Logo Argenflix"></a>
      </figure>
    <h1 class="titulo">Agregar Actor/Actriz :</h1>
    <br>
    <section class="formulario">
        <form action="/actors/create" method="POST">
            <label for="first_name">Nombre :</label>
            <p><input type="text" name="first_name" id="" value="<%= locals.old && old.first_name ? old.first_name : null %>" required></p>
            <p style="color:red"><%= locals.errores && errores.first_name ? errores.first_name.msg : null %> </p>
            
            <label for="last_name">Apellido :</label>
            <p><input type="text" name="last_name" id="" value="<%= locals.old && old.last_name ? old.last_name : null %>" required></p>
            <p style="color:red"><%= locals.errores && errores.last_name ? errores.last_name.msg : null %> </p>

            <label for="rating">Rating :</label>
            <p><input type="number" step="0.1" name="rating" id="" value="<%= locals.old && old.rating ? old.rating : null %>" required> </p>
            <p style="color:red"><%= locals.errores && errores.rating ? errores.rating.msg : null %> </p>
            
            <p>
                <select name="favorite_movie_id" id="" required>
                    <option value="" disabled selected>- Película favorita del catálogo - </option>
                    <% movies.forEach(movie => { %>
                    <option value="<%= movie.id %>"><%= movie.title %></option>
                    <% }); %>
                </select>
            </p>
           
           
            <br>
            <p> <button class="botonAgregar" type="submit">Agregar</button>
            
            <a  class="botonVolver" href="/actors">Listado de Actores</a></p>
        </form>
    </section>
    
</body>

</html>